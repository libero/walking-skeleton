server {
    server_name localhost;
    listen 80;
    root /srv/app;

    types {
        application/xml rng;
    }

    location = /favicon.ico { access_log off; log_not_found off; }

    location / {
        autoindex on;
    }
}
