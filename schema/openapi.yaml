openapi: 3.0.1

info:
    title: Libero walking skeleton
    license:
        name: MIT

servers:
    - url: http://localhost:8085

paths:

    /articles:
        $ref: articles.yaml#paths/~1

    /articles/{id}:
        $ref: articles.yaml#paths/~1{id}

    /articles/{id}/{version}:
        $ref: articles.yaml#paths/~1{id}~1{version}

    /articles/{id}/{version}/{part}:
        $ref: articles.yaml#paths/~1{id}~1{version}~1{part}
